
tags:
  # Test the package version self-references.
  version:
    timeout: 10s
  # Tests that uses entities.
  entities:
    timeout: 180s
  # Tests that uses `Docker` containers.
  docker:
    timeout: 180s
  # Tests that uses `PostgreSQL`.
  postgres:
    timeout: 180s
  # Tests that uses `MySQL`.
  mysql:
    timeout: 180s
  # Tests that depends on `build` framework:
  build:
    timeout: 60s

